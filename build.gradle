apply plugin: 'java'
apply plugin: 'eclipse'

sourceCompatibility = //choose whatever version you want -1.6 or above.
version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'Gradle Quickstart',
                   'Implementation-Version': version
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // Test Compile Dependencies : 
    // junit, io.rest-assured, org.hamcrest
    
    // Code Compile Dependencies
    // com.google.code.gson'
    // commons-collections'
}

test {
    systemProperties 'property': 'value'
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}
